{
  "name": "vibin",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "description": "VIBIN — find your vibe, make a plan. Groups swipe on activities and only match when everyone agrees.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "concurrently -k -n vite,worker -c magenta,cyan \"npm:dev:client\" \"npm:dev:worker\"",
    "dev:client": "vite",
    "dev:worker": "wrangler dev",
    "build": "vite build",
    "typecheck": "tsc -p tsconfig.client.json --noEmit && tsc -p tsconfig.worker.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:integration": "node tests/integration.mjs",
    "smoke": "powershell -ExecutionPolicy Bypass -File scripts/smoke.ps1",
    "db:generate": "drizzle-kit generate",
    "db:migrate:local": "wrangler d1 migrations apply vibin-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply vibin-db --remote",
    "db:seed:build": "tsx seed/build-seed.ts",
    "db:seed:local": "npm run db:seed:build && wrangler d1 execute vibin-db --local --file=./seed/seed.sql",
    "db:seed:remote": "npm run db:seed:build && wrangler d1 execute vibin-db --remote --file=./seed/seed.sql",
    "deploy": "npm run build && wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "hono": "^4.6.14",
    "drizzle-orm": "^0.36.4",
    "nanoid": "^5.0.9",
    "zod": "^3.24.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.28.1"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.40",
    "@cloudflare/workers-types": "^4.20241218.0",
    "@types/react": "^18.3.18",
    "@types/react-dom": "^18.3.5",
    "@vitejs/plugin-react": "^4.3.4",
    "autoprefixer": "^10.4.20",
    "concurrently": "^9.1.0",
    "drizzle-kit": "^0.28.1",
    "eslint": "^9.17.0",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.17",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vite": "^6.0.5",
    "vitest": "^2.1.8",
    "wrangler": "^3.95.0"
  }
}
